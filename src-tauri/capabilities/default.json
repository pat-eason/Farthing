{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and popover windows",
  "windows": ["main", "popover"],
  "permissions": ["core:default", "opener:default", "dialog:allow-save", "notification:default"]
}
